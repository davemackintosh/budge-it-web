import React from "react"
import { ReportContainer } from "@src/shared/theme/report"

export const Graph404 = (): JSX.Element => (
  <ReportContainer>
    <p>Whoops, youve found a report that doesnt exist!</p>
  </ReportContainer>
)
